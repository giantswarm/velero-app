apiVersion: v1
kind: Service
metadata:
  name: {{ include "velero-app.fullname" . }}
  labels:
    {{- include "velero-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "velero-app.selectorLabels" . | nindent 4 }}
