apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "velero-app.fullname" . }}-test-connection"
  labels:
    {{- include "velero-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "velero-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
